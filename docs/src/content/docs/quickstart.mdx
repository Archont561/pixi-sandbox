---
title: Quickstart
description: Build a sandbox bundle and publish it from CI — with icons from Iconify.
---

import { Icon } from 'astro-icon/components';
import { Tabs, TabItem } from '@astrojs/starlight/components';

## On your machine

<div style="display: flex; gap: 0.5rem; align-items: center; margin-bottom: 0.5rem;">
  <Icon name="lucide:terminal" size={18} />
  <strong>Clone and test</strong>
</div>

```bash
git clone https://github.com/Archont561/pixi-sandbox && cd pixi-sandbox
pixi run -e dev test          # or: pixi run -e dev lint

# End-to-end proof (bare checkout, allow few minutes):
pixi run -e dev sandbox-proof
```

## In your project

<Tabs>
  <TabItem label="Pack">
    ```bash
    # 1. Inspect helper-tool pins
    pixi-sandbox tools list
    # Optional org mirror:
    # pixi-sandbox tools list --tools-lock /path/to/reviewed-tools.lock.json

    # 2. Pack envs + vendored crates + tools
    cargo build -p pixi-sandbox --release
    pixi-sandbox pack \
      --repo-root . --envs "dev,docs" \
      --output-dir .sandbox-transport \
      --fetch-tools --cargo-vendor \
      --self-bin target/release/pixi-sandbox

    # Or use verified release binary:
    # pixi-sandbox pack ... --self-bin pixi-sandbox-x86_64-unknown-linux-musl
    ```
  </TabItem>
  <TabItem label="Verify">
    ```bash
    # 3. Verify (CI gate, writes nothing)
    pixi-sandbox doctor --branch-location .sandbox-transport --verify
    ```
  </TabItem>
  <TabItem label="Publish">
    ```bash
    # 4. Publish as orphan branch
    pixi-sandbox publish --input-dir .sandbox-transport --branch-name sandbox/dev-linux-64
    ```
  </TabItem>
</Tabs>

## On the airlock

An extracted orphan branch is self-contained. The root `pixi-sandbox` is a convenience copy of
the manifest-verified binary under `.pixi-sandbox/tools/<platform>/`; Git stores identical copies
as one blob. A bare invocation is deliberately read-only: it runs `doctor --verify` and prints
the next restore command.

```bash
cd /path/to/extracted-branch
./pixi-sandbox
./restore.sh /path/to/project
# equivalent explicit form:
./pixi-sandbox restore --branch-location . --output-path /path/to/project --force
```

Windows branches provide the matching `pixi-sandbox.exe` and `restore.ps1`.

## From CI: reviewed native matrix

<div style="display: flex; gap: 0.5rem; align-items: center; margin: 1rem 0;">
  <Icon name="lucide:settings" size={18} />
  <strong>Declare exactly what to publish</strong>
</div>

Don't infer every env from `pixi.toml` — projects have private test/bench envs.

```toml
# .pixi-sandbox.toml
schema = 1
branch_prefix = "sandbox"
cargo_vendor = true

[[bundle]]
name = "developer"
environments = ["dev", "docs"]
platforms = ["linux-64", "osx-arm64", "win-64"]
```

Validate locally:

```bash
pixi-sandbox plan --config .pixi-sandbox.toml
pixi-sandbox plan --config .pixi-sandbox.toml --json  # GitHub Actions matrix
```

Your workflow just uses the two actions — your `uses: @v0.2.0` pin is the version, no `release-repository` needed.

<Tabs>
  <TabItem label="Two actions (recommended)">
    ```yaml
    # .github/workflows/publish-sandbox.yml in your project
    name: publish sandbox
    on: { push: { branches: [main] }, workflow_dispatch: {} }
    jobs:
      plan:
        runs-on: ubuntu-latest
        outputs: { matrix: ${{ steps.plan.outputs.matrix }} }
        steps:
          - uses: actions/checkout@v7.0.1
          - uses: Archont561/pixi-sandbox/setup@v0.2.0
            id: setup
            with: { version: v0.2.0 }
          - id: plan
            run: echo "matrix=$(${{ steps.setup.outputs.path }} plan --config .pixi-sandbox.toml --json)" >> $GITHUB_OUTPUT
      publish:
        needs: plan
        strategy: { matrix: ${{ fromJSON(needs.plan.outputs.matrix) }}, fail-fast: false }
        runs-on: ${{ matrix.runner }}
        steps:
          - uses: actions/checkout@v7.0.1
          - uses: prefix-dev/setup-pixi@v0.10.2
          - uses: Archont561/pixi-sandbox/setup@v0.2.0
            id: setup
            with: { version: v0.2.0 }
          - uses: Archont561/pixi-sandbox/publish@v0.2.0
            with:
              project: .
              environments: ${{ matrix.environments }}
              platform: ${{ matrix.platform }}
              branch: ${{ matrix.branch }}
              self-bin: ${{ steps.setup.outputs.path }}
              remote: https://github.com/${{ github.repository }}.git
              output-dir: /tmp/transport
              push-token: ${{ secrets.GITHUB_TOKEN }}
    ```
  </TabItem>
  <TabItem label="Single platform">
    ```yaml
    - uses: Archont561/pixi-sandbox/setup@v0.2.0
      id: setup
      with: { version: v0.2.0 }
    - uses: Archont561/pixi-sandbox/publish@v0.2.0
      with:
        project: .
        environments: dev
        platform: linux-64
        branch: sandbox/dev-linux-64
        self-bin: ${{ steps.setup.outputs.path }}
        remote: https://github.com/OWNER/REPO.git
        output-dir: /tmp/transport
    ```
  </TabItem>
</Tabs>

<details>
<summary>Alternative: reusable workflow</summary>

```yaml
jobs:
  sandbox:
    uses: Archont561/pixi-sandbox/.github/workflows/publish-sandbox.yml@<sha>
    with:
      config: .pixi-sandbox.toml
      release-repository: Archont561/pixi-sandbox
      release-version: v0.2.0
    secrets:
      SANDBOX_PUSH_TOKEN: ${{ secrets.SANDBOX_PUSH_TOKEN }}
```

Needs duplicate `release-*` inputs — prefer two actions above.

</details>

<div style="display: flex; gap: 0.5rem; padding: 0.75rem; background: var(--sl-color-gray-6); border-radius: 0.5rem; margin: 1rem 0;">
  <Icon name="lucide:info" size={18} />
  <span>A matrix job only publishes its matching native platform. <code>osx-arm64</code> must use Apple-Silicon runner; publish rejects mismatched runner rather than cross-packing unproven transport. Use <code>[runners]</code> in <code>.pixi-sandbox.toml</code> for self-hosted labels. Runner override must have embedded pins for <code>pixi</code>, <code>pixi-pack</code>, <code>pixi-unpack</code>.</span>
</div>

## Direct actions (like setup-pixi)

Both live in same repo:

```yaml
# Setup
- uses: Archont561/pixi-sandbox@v0.2.0
  with:
    version: v0.2.0
- run: pixi-sandbox --version

# Explicit short paths
- uses: Archont561/pixi-sandbox/setup@v0.2.0
  id: setup
  with:
    version: v0.2.0
- uses: Archont561/pixi-sandbox/publish@v0.2.0
  with:
    project: .
    environments: dev
    platform: linux-64
    branch: sandbox/dev-linux-64
    self-bin: ${{ steps.setup.outputs.path }}
    remote: https://github.com/OWNER/REPO.git
    output-dir: /tmp/transport

# Long-form (pin SHA for supply-chain)
- uses: Archont561/pixi-sandbox/.github/actions/setup-pixi-sandbox@<sha>
- uses: Archont561/pixi-sandbox/.github/actions/publish-pixi-sandbox@<sha>
```

Verifies bytes against `SHA256SUMS` before making executable available on PATH. Generate checksums after native proof: `(cd release && sha256sum pixi-sandbox-* > SHA256SUMS)`. `latest` warns; pin in prod.

## Shipping the tool (.conda)

```bash
pixi publish --path crates/pixi-sandbox --build-dir .pixi/bld --target-dir dist
pixi publish --path crates/pixi-sandbox --build-dir .pixi/bld --target-channel file:///srv/channel
pixi global install -c file:///srv/channel -c conda-forge pixi-sandbox
pixi sandbox doctor --branch-location .sandbox-transport
```

Measured: 34s to build 594 KB package (1.26 MiB stripped binary), channel write produces `repodata.json` + `.zst` + sharded repodata — plain static host (Pages, S3) works as channel. Must combine with `-c conda-forge`.

## Sizes to expect

Measured small project (2 envs, 33 crates, linux-64):

| Part | Payload |
|------|------|
| conda envs (.conda channel) | 133 MB |
| cargo vendor tree (loose) | 33 MB |
| bundled tools (pixi, pixi-unpack, self) | 96 MB |
| **transport dir** | **262 MB** |
| **orphan branch after git dedup** | **~110 MB** |

Tools dominate small bundles — expected, git stores each tool blob once.
